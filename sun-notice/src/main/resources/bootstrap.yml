spring:
  application:
    name: sun-notice
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        refreshable-dataids: true